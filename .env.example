# LiteLLM API port on host
LITELLM_PORT=4000

# Direct path to llama-server binary. Leave empty to auto-detect.
LLAMA_SERVER_BIN=

# Postgres for LiteLLM UI/admin features
POSTGRES_DB=litellm
POSTGRES_USER=litellm
POSTGRES_PASSWORD=litellm
DATABASE_URL=postgresql://litellm:litellm@postgres:5432/litellm

# Master API key — используется в Authorization: Bearer <key>
# Задай свой ключ перед использованием
LITELLM_MASTER_KEY=sk-local-change-me

# UI credentials — логин/пароль для http://localhost:4000/ui
UI_USERNAME=admin
UI_PASSWORD=changeme

# Open WebUI
OPENWEBUI_PORT=3000
WEBUI_AUTH=False

# По умолчанию Open WebUI ходит напрямую в llama.cpp (fast)
OPENWEBUI_API_BASE_URL=http://host.docker.internal:8080/v1
OPENWEBUI_API_KEY=dummy

# Если хочешь ходить через LiteLLM, используй:
# OPENWEBUI_API_BASE_URL=http://litellm:4000/v1
# OPENWEBUI_API_KEY=sk-local-change-me
